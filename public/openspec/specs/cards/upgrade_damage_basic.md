# ⛔ DEPRECATED — Replaced by `damage.md`

This example used hypothetical numbers. Real code in `src/progression/UpgradeCards.ts` uses id `damage` and value `+10` flat (not ×1.20). See `damage.md` for canon spec.
